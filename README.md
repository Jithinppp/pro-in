todos

1. focus on equipments table add, delete, update etc
   for that inv manager page to be finished
2. then delete event from pm side


todo for create event pm 
select disable setup date only enable after event date added
also remove the checkbox add keep add multiple date button 
same remove the checkbox for venues keep the add venue button